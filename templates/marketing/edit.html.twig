{% extends 'base.html.twig' %}

{% block title %}Edit Marketing{% endblock %}

{% block body %}
    <h1>Edit Marketing</h1>

    {{ include('marketing/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('marketing_index') }}">back to list</a>

    {{ include('marketing/_delete_form.html.twig') }}
{% endblock %}
